<UserControl x:Class="IGAntiRansomwareUI.RulesPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#1e1e2e">

    <UserControl.Resources>
        <!-- Definição do SidebarButtonStyle -->
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFCDD6F4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,15"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Width" Value="200"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="contentPresenter"
                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                      TextBlock.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF181880"/>
                                <Setter TargetName="contentPresenter" Property="TextBlock.Foreground" Value="#FFFFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF1a1a1a"/>
                                <Setter TargetName="contentPresenter" Property="TextBlock.Foreground" Value="#FFFFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#FF444444"/>
                                <Setter TargetName="contentPresenter" Property="TextBlock.Foreground" Value="#FF888888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilos adicionais para o RulesPanel -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFCDD6F4"/>
            <Setter Property="Margin" Value="0,5,0,5"/>
        </Style>

        <Style TargetType="Button" x:Key="ActionButtonStyle">
            <Setter Property="Background" Value="#FF89B4FA"/>
            <Setter Property="Foreground" Value="#FF1E1E2E"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style TargetType="Border" x:Key="CardStyle">
            <Setter Property="Background" Value="#FF313244"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="10"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Cabeçalho -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="20,20,20,10">
            <Button Content="← Voltar" Click="BtnBack_Click"
                    Style="{StaticResource SidebarButtonStyle}" Width="100"/>

            <TextBlock Text="Gerenciamento de Regras" 
                       FontSize="18" FontWeight="Bold"
                       Foreground="White" Margin="20,0,0,0"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Conteúdo -->
        <Grid Grid.Row="1" Margin="20,0,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="400"/>
            </Grid.ColumnDefinitions>

            <!-- Lista de Regras -->
            <Border Grid.Column="0" Background="#FF313244" 
                    CornerRadius="5" Margin="0,0,10,0"
                    Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Botões -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" 
                                Margin="10" HorizontalAlignment="Right">
                        <Button Content="Nova Regra" Click="BtnNewRule_Click"
                                Style="{StaticResource ActionButtonStyle}"/>
                        <Button Content="Aplicar ao Driver" Click="BtnApplyRules_Click"
                                Style="{StaticResource ActionButtonStyle}"
                                Margin="10,0,0,0" Background="#FFA6E3A1"/>
                    </StackPanel>

                    <!-- Lista -->
                    <ListView Grid.Row="1" x:Name="lstRules" 
                              Background="Transparent" Foreground="White"
                              BorderThickness="0" SelectionChanged="LstRules_SelectionChanged">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Nome" Width="200" 
                                                DisplayMemberBinding="{Binding Name}"/>
                                <GridViewColumn Header="Tipo" Width="100" 
                                                DisplayMemberBinding="{Binding Type}"/>
                                <GridViewColumn Header="Ativa" Width="60">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsActive}" 
                                                      IsEnabled="False"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <!-- Status -->
                    <TextBlock Grid.Row="2" x:Name="txtRulesStatus" 
                               Margin="10" Foreground="#FFA6E3A1"/>
                </Grid>
            </Border>

            <!-- Editor de Regras -->
            <Border Grid.Column="1" Background="#FF313244" 
                    CornerRadius="5" Visibility="Collapsed"
                    x:Name="editorContainer"
                    Style="{StaticResource CardStyle}">
                <ContentControl x:Name="ruleEditorHost"/>
            </Border>
        </Grid>
    </Grid>
</UserControl>